<div class="card col-12 col-sm-2 col-md-4 col-lg-3 p-0 mx-2" style="max-width: 260px;">
  <%= link_to product_path(alias: product.alias) do %>
    <%= image_tag(product.photo, class: 'card-img-top', style: "max-height: '200'") %>
    <div class="card-body text-dark">
      <p class="card-text mb-0 font-weight-bold"><%= product.name %></p>
      <h4 class="card-title mb-0"><%= number_to_currency(product.price, unit: 'R$', separator: ',', delimiter: "") %></h4>
      <p class="card-text"><%= product.description %></p>
    </div>
  <% end %>
</div>